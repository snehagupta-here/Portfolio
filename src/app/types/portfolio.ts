export type PersonalInfo = {
  name: string;
  title: string;
  tagline: string;
  bio: string;
  email: string;
    avatar : string;
    aboutImage?: string;
};

export type SocialLink = {
  name: string;
  url: string;
  icon: string;
};
